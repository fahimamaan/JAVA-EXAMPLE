package com.spring;


//@Configuration
//@EnableWebSecurity
public class SecurityConfig {/*

	
	@Override
	public void configure(AuthenticationManagerBuilder auth) throws Exception{
		
		auth.inMemoryAuthentication().withUser("fahim").password("aaman").roles("USER");
		
	}
	
	@Override
	public void configure(HttpSecurity security) throws Exception{
		
		security.antMatcher("/**").authorizeRequests().anyRequest().hasRole("USER")
		.and().formLogin().loginPage("/login.jsp").failureForwardUrl("/login.jsp")
		.loginProcessingUrl("login").permitAll().and().logout().logoutSuccessUrl("/userinfo");
	}
*/}
